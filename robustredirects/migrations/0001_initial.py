# Generated by Django 2.2.24 on 2021-11-23 18:05

from django.db import migrations, models
import django.db.models.deletion


class Migration(migrations.Migration):

    initial = True

    dependencies = [
        ('sites', '0002_alter_domain_unique'),
    ]

    operations = [
        migrations.CreateModel(
            name='Redirect',
            fields=[
                ('id', models.AutoField(auto_created=True, primary_key=True, serialize=False, verbose_name='ID')),
                ('from_url', models.CharField(db_index=True, help_text="Absolute path, excluding the domain. Example: '/about/'", max_length=255, unique=True, verbose_name='From URL')),
                ('to_url', models.CharField(blank=True, db_index=True, help_text="Absolute or relative url. Examples: 'http://www.example.com', '/something/'", max_length=255, verbose_name='To URL')),
                ('http_status', models.SmallIntegerField(choices=[(301, '301 - Permanent Redirect'), (302, '302 - Temporary Redirect')], default=301, verbose_name='HTTP Status')),
                ('status', models.BooleanField(choices=[(True, 'Active'), (False, 'Inactive')], default=True)),
                ('is_partial', models.BooleanField(default=False, help_text='The From and To URL are partial. They will be used if they partially match any part of the url. Can not be a regular expression.', verbose_name='Is a partial url')),
                ('uses_regex', models.BooleanField(default=False, help_text='Check if the From URL uses a regular expression. If so, it will be processed in a urlconf.', verbose_name='Uses Regular Expression')),
                ('create_dt', models.DateTimeField(auto_now_add=True)),
                ('update_dt', models.DateTimeField(auto_now=True)),
                ('site', models.ForeignKey(on_delete=django.db.models.deletion.CASCADE, related_name='robust_redirects', to='sites.Site')),
            ],
            options={
                'verbose_name': 'redirect',
                'verbose_name_plural': 'redirects',
                'ordering': ('-uses_regex',),
                'unique_together': {('site', 'from_url')},
            },
        ),
    ]
